export const API_CONFIG = {
  BASE_URL: 'https://api.example.com',  // Your API base URL here

  ENDPOINTS: {
    GET_ITEMS: '/items',              // Example endpoint
    // Add more endpoints if needed
  }
};
